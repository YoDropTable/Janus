services:
  janus:
    build: .
    ports:
      - "${JANUS_PORT:-8080}:8080"
    environment:
      JANUS_DB_PATH: /data/janus.db
    volumes:
      - janus-data:/data
    restart: unless-stopped

volumes:
  janus-data:
